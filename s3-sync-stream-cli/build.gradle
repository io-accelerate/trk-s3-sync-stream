plugins {
    id('java')
    id('application')
    id('java-library')
    id('signing')
    id('maven-publish')
    id('com.github.johnrengelman.shadow') version '8.1.1'
}

application {
    mainClass = "tdl.s3.SyncFileApp"
}

apply from: project(':build-logic').file('shared.gradle')
apply from: project(':build-logic').file('application.gradle')
apply from: project(':build-logic').file('publish.gradle')

dependencies {
    implementation(project(':s3-sync-stream-lib'))
}

